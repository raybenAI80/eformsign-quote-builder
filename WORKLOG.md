# 작업 기록

## 2025-11-21
- ItemEditor 한글 복구 및 재작성: 빠른 추가를 문서/추가 옵션 두 그룹으로 분리하고 모든 라벨·토스트·placeholder를 UTF-8로 정상화.
- 할인/비고 UX 개선: 할인은 슬라이더+직접 입력+프리셋(0/30/60/100) 지원, 비고는 4줄 textarea로 확대.
- 반복 추가 제거: 빠른 추가는 항상 1개씩만 추가하도록 변경하고 반복 UI 삭제.
- 추가 옵션 데이터 정리: 표 기준 단가 반영(SMS 알림 100, 이메일 인증 20, 개인/법인 인증 50, 타임스탬프 30, 공전소 100, 카카오톡 브랜드 500000, SMS 발신번호 500000, 이메일 발송주소 1000000). 빠른 추가 버튼에 정상 표시되도록 섹션 값 포함.
- 레이아웃/스텝바/검증: MainLayout 스크롤 고정, 스텝바 칩 개선, 내보내기 전 필수값 검증 토스트 적용.

## 2025-11-24
- 워터마크 구현: 견적서 미리보기 본문에 이폼사인 로고 워터마크 추가.
- 워터마크 배치 개선: 초기 중앙 배치에서 하단 배치로 변경 후, 최종적으로 "2단 대각선 배치"(-30도 회전, 상단 35%/하단 75%)로 확정하여 전문성 강화.
- 상단 로고 제거: 헤더 영역의 중복된 회색 로고 제거.
- 버그 수정: BasicInfoEditor.tsx의 구문 오류로 인한 렌더링 문제 해결.
- **옵션 & 히스토리 UI 개선**: 브랜딩 모드 선택을 카드 UI로 개선, 프리셋/히스토리 목록에 요약 정보(총액, 품목 수) 추가.
- **AI 견적서 기본값 설정**: 새 견적서 생성 시 'AI 견적서' 모드가 기본으로 선택되도록 변경.
- **AI 타이틀 버그 수정**: 제목이 비어있을 때 "AI"만 표시되던 문제 수정 → "이폼사인 AI 견적서" 정상 표시.
- **탭 순서 재구성**: 사용자 경험 개선을 위해 '옵션 설정'을 첫 번째 탭으로 이동 (옵션 설정 → 기본 정보 → 품목 카드 → 기록).
- **브랜딩 라벨 업데이트**: AI 견적서 - 'AI 초거대 지원과제', 일반 견적서 - '이폼사인 SaaS', 공공 견적서 - '이폼사인 CSAP'으로 명칭 변경.
- **섹션 제목 변경**: 미리보기 패널에서 'SaaS' → '문서', 'Service' → '추가 옵션'으로 표시되도록 매핑 적용.
- **UI 버그 수정**: 품목 카드 탭 하단의 '위로' 버튼이 흰색 배경으로 인해 보이지 않던 문제 수정 (배경색을 `bg-slate-900`으로 명시적 지정).
- **기능 이동**: '템플릿 저장' 기능을 '옵션 설정' 탭에서 '버전 관리' 탭으로 이동.
    - 옵션 설정: 템플릿 불러오기/삭제 기능만 유지 (시작점 역할 강화).
    - 버전 관리: '템플릿으로 저장' 섹션 추가 (작업 완료 후 저장 역할 강화).
- **UX 개선**: 휴대전화 번호 입력 시 4자리 입력 후 자동으로 하이픈(-)이 삽입되도록 개선 (예: 12345678 → 1234-5678).
- **기능 추가**: 버전 관리 탭에 '개별 삭제' 및 '미리보기' 기능 추가.
    - 개별 삭제: 각 버전 항목에 휴지통 아이콘 추가, 삭제 시 확인 팝업 제공.
    - 미리보기: 각 버전 항목에 눈 아이콘 추가, 클릭 시 우측 패널에 해당 버전 내용 표시 (편집 중인 내용은 유지).
- **버그 수정**: 견적 템플릿 삭제 버튼이 일부 환경에서 동작하지 않는 문제 수정 (Native Confirm → Custom Modal 변경).
- **기능 추가**: 템플릿 불러오기 시 옵션 제공.
    - 새로 작성하기: 품목/고객 정보만 가져오고 날짜/번호는 초기화.
    - 이어서 작성하기: 저장된 시점의 모든 데이터를 그대로 복원.

## 2025-12-15
- **OCR PDF 기능 구현**: html2canvas 이미지 기반 PDF에 투명 텍스트 레이어 추가하여 텍스트 검색/복사 가능.
  - `fontLoader.ts`: NanumSquare 폰트 CDN 로드 + jsPDF 등록.
  - `textExtractor.ts`: Range API로 문자별 Y 위치 감지하여 CSS 래핑 줄바꿈 처리.
  - `exportPdf.ts`: 투명 텍스트 오버레이 (opacity 0.01), baseline 오프셋 보정.
- **PDF 페이지 크기 최적화**: 고정 400mm에서 콘텐츠 높이에 맞춤 → 하단 여백 제거.
- **빈 페이지 버그 수정**: 다중 페이지 조건 `>= 0` → `> 0`으로 변경.
- **백업**: `backup_20251215_ocr_pdf/` 폴더에 관련 파일 및 CHANGELOG.md 저장.

## 2025-12-16
- **PDF A4 인쇄 최적화**: 
  - 단일 페이지 PDF (동적 높이)로 변경하여 OCR/링크 정상 작동 유지.
  - PDF 너비 210mm → 290mm로 확대하여 "용지에 맞춤" 인쇄 시 A4에 꽉 차게 출력.
- **레이아웃 세로 간격 축소**:
  - 헤더 여백: mb-12 → mb-6, pb-6 → pb-4.
  - 날짜 영역: gap-12 → gap-8, mb-12 → mb-6.
  - 테이블 행: py-4 → py-2.
- **사이드바 레이아웃 조정**:
  - 사이드바 너비: 34% → 28% (메인 컨텐츠에 더 많은 공간).
  - 좌우 여백 조정: pl-1 pr-3 (회색 영역), ml-1 mr-3 (파란색 영역).
  - 메인 컨텐츠 패딩: 10mm → 5mm.
- **슬로건 이미지 교체**: 새 프로모션 이미지로 `/promo-full.png` 업데이트.
- **AI 아이콘 교체 및 정렬**:
  - AI 아이콘을 새 디자인 (별+그라데이션)으로 교체.
  - "견적서" 제목과 AI 아이콘 배치 개선: absolute → inline-flex, h-14 → h-10.
- **이폼사인 로고 크기 조정**: w-44 → w-36.
